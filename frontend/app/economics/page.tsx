/**
 * Economics page — Phase 8D will build this out.
 * Placeholder so the /economics route doesn't 404.
 */
export default function EconomicsPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p style={{ color: "var(--text-secondary)" }}>Economics — coming soon</p>
    </div>
  );
}
